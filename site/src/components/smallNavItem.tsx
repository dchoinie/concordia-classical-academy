import React from 'react'
import { NavItemType } from '../types/common'

const SmallNavItem = ({ label, link, subLinks }: NavItemType) => {
  return (
    <div>label</div>
  )
}

export default SmallNavItem;